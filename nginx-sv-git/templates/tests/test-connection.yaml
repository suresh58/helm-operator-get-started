apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-sv-git.fullname" . }}-test-connection"
  labels:
{{ include "nginx-sv-git.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "nginx-sv-git.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
